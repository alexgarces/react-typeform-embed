import React from 'react';
import { ReactTypeformEmbed } from '../components';

const ExampleWidget = () => (
  <div className="ExampleWidget">
    <ReactTypeformEmbed popup={false} id="njdbt5" height={400} />
  </div>
);

export default ExampleWidget;
